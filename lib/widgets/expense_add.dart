import 'package:flutter/material.dart';

class ExpenseAdd extends StatelessWidget {
  const ExpenseAdd({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
